---
// src/pages/index.astro
// This page is reached if the middleware decides not to redirect or as a static fallback.
// In SSR mode with Cloudflare, the middleware handles the redirection to /[lang]/.
---
