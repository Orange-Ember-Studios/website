import type { FC } from "@emberkit/core";

const Home: FC = () => (
  <main className="max-w-4xl mx-auto p-8">
    <h1 className="text-3xl font-bold text-ember-500">Orange Ember Studios</h1>
    <p className="mt-4 text-gray-300">EmberKit SPA — expand routes under <code className="text-ember-300">src/routes/</code> as you migrate legacy pages.</p>
  </main>
);

export default Home;
